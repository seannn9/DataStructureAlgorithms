#include <iostream>
#include "group.h"

using namespace std;

int main()
{
    group group1(100, 90, 100);
    group group2(100, 100, 100);
    group group3(90, 100, 100);
    group::Result();
    return 0;
}
